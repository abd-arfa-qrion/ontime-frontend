import AdminDashboardView from "@/components/view/admin/Dashboard";

const AdminPage = ({ setToaster }: any) => {
  return <AdminDashboardView setToaster={setToaster} />;
};

export default AdminPage;
